# projexor

A foundation for building code-aware tools that exposes project structure, AST data, file operations, and command execution as clean, composeable functions any program can import and use.
